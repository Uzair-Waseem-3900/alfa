# Performance Review — Complexity Analysis

For every important operation, identify its approximate time complexity.

Make sure the plan explicitly includes everything that is not O(1), especially O(N), O(N log N), O(N²), etc., and explain how it will behave as data grows.
